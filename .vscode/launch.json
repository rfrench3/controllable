{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run bazzite_updater",
            "type": "node-terminal",
            "request": "launch",
            "command": "${workspaceFolder}/build/bin/bazzite_updater",
            "cwd": "${workspaceFolder}",
            "env": {}
        }
    ]
}